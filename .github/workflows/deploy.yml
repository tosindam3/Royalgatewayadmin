name: Fresh Deploy to Hostinger via Rsync

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install NPM Dependencies
        run: npm ci

      - name: 🏗️ Build React App
        env:
          VITE_API_BASE_URL: https://www.royalgatewayadmin.com
          NODE_ENV: production
        run: npm run build
        
      - name: 📤 Deploy to Hostinger via SSH/Rsync
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.HOSTINGER_SSH_KEY }}
          REMOTE_HOST: ${{ secrets.HOSTINGER_SSH_HOST }}
          REMOTE_USER: ${{ secrets.HOSTINGER_SSH_USERNAME }}
          REMOTE_PORT: ${{ secrets.HOSTINGER_SSH_PORT }}
          TARGET: "domains/royalgatewayadmin.com/public_html"
          ARGS: "-avz --checksum --delete"
          SOURCE: "dist/"
          EXCLUDE: "/.htaccess, /index.php, /.env"

      - name: 🚀 Disable index.html Caching
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTINGER_SSH_HOST }}
          username: ${{ secrets.HOSTINGER_SSH_USERNAME }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_SSH_PORT }}
          script: |
            HTACCESS="domains/royalgatewayadmin.com/public_html/.htaccess"
            if ! grep -q "index.html" "$HTACCESS"; then
              echo -e "\n<FilesMatch \"index\.html$\">\n    FileETag None\n    <IfModule mod_headers.c>\n        Header unset ETag\n        Header set Cache-Control \"max-age=0, no-cache, no-store, must-revalidate\"\n        Header set Pragma \"no-cache\"\n        Header set Expires \"Wed, 11 Jan 1984 05:00:00 GMT\"\n    </IfModule>\n</FilesMatch>" >> "$HTACCESS"
              echo "✅ Cache-control added to .htaccess"
            else
              echo "ℹ️ Cache-control already exists in .htaccess"
            fi

      - name: 🔄 Post-Deployment Backend Update
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOSTINGER_SSH_HOST }}
          username: ${{ secrets.HOSTINGER_SSH_USERNAME }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_SSH_PORT }}
          script: |
            set -e
            echo "🔄 Starting backend update process..."
            cd /home/u237094395/apps/royalgatewayadmin/backend
            
            # Optimization: Pull latest repo changes for backend
            cd ..
            # Update remote URL to use token for authentication
            git remote set-url origin https://x-access-token:${{ secrets.DEPLOY_TOKEN_PAT }}@github.com/tosindam3/Royalgatewayadmin.git
            git pull origin main
            
            cd backend
            composer install --no-dev --optimize-autoloader --no-interaction
            php artisan migrate --force
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            
            chmod -R 775 storage bootstrap/cache
            echo "✅ Backend updated successfully!"

      - name: 🩺 Final Health Check
        run: |
          sleep 5
          curl -f https://www.royalgatewayadmin.com/api/health || echo "Health check failed, please check manually."
